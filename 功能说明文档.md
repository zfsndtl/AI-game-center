# AI 游戏中心 — 功能说明文档

## 一、项目概述

本项目是一个**纯前端小游戏与工具集合**，基于 HTML5 + CSS3 + 原生 JavaScript 构建，零依赖、无框架、无构建工具。包含 4 款小游戏和 2 个实用工具，通过本地 HTTP 服务器统一访问。

**技术特点：**
- 纯原生 JavaScript，无任何第三方依赖
- 响应式设计，支持桌面与移动端
- 使用 localStorage 实现跨标签页通信与数据持久化
- 动态背景系统 + 气球/气泡粒子交互特效

---

## 二、项目结构

```
AI-game-center/
├── index.html                    # 主页（导航入口）
├── gomoku-home.html              # 五子棋游戏页
├── go.html                       # 围棋游戏页
├── sheep.html                    # 消消乐（挑战模式）
├── sheep-simple.html             # 消消乐（简易模式）
├── football.html                 # 足球比分预测
├── calculator.html               # 计算器
├── start.bat                     # 本地服务器启动脚本
├── js/
│   ├── game.js                   # 五子棋核心逻辑
│   ├── go.js                     # 围棋核心逻辑
│   ├── sheep.js                  # 消消乐挑战模式逻辑
│   ├── sheep-simple.js           # 消消乐简易模式逻辑
│   ├── football.js               # 足球预测引擎
│   ├── calculator.js             # 计算器逻辑
│   ├── background-manager.js     # 动态背景管理
│   └── balloon-bubble-interaction.js  # 气球/气泡交互特效
├── css/
│   ├── style.css                 # 主页样式
│   ├── background.css            # 背景通用样式
│   ├── calculator.css            # 计算器样式
│   ├── football.css              # 足球预测样式
│   ├── go.css                    # 围棋样式
│   ├── sheep.css                 # 消消乐挑战模式样式
│   └── sheep-simple.css          # 消消乐简易模式样式
├── assets/
│   └── backgrounds/              # 5 张风景背景图
└── server/                       # 服务器目录（保留）
```

---

## 三、功能模块详解

### 3.1 五子棋（Gomoku）

**文件：** `gomoku-home.html` + `js/game.js`（1206 行）

#### 功能说明

| 功能 | 说明 |
|------|------|
| 人人对战（本地） | 同一设备两人轮流落子 |
| 人机对战 | 玩家 vs 启发式 AI |
| 在线对战 | 基于跨标签页通信的房间制联机 |

#### 核心实现

- **棋盘：** 15×15 标准棋盘，Canvas 绘制
- **AI 算法：** 启发式评估函数，候选位置为已有棋子周围 2 格范围内
  - 评分函数 `scoreByCountAndOpen(count, open)`：连五 = 100000，活四 = 10000，活三 = 1000，眠四 = 1000，活二 = 100，眠三 = 100
  - AI 同时评估进攻与防守分值，取最高分位置
- **在线联机：**
  - 通信机制：`localStorage` 轮询（300ms 间隔）
  - 房间码：4 位数字
  - 对手发现：写入 `gomoku-declare-{code}-{peerId}` 键（10 秒过期）
  - 消息传递：`gomoku-msg-{code}-{senderId}-{type}-{seq}` 键
  - 颜色分配：按 peerId 排序，index 0 = 黑棋（先手）
- **悔棋：** 人人/在线模式退 1 步，人机模式退 2 步（撤回 AI + 玩家各一步）

---

### 3.2 围棋（Go / Weiqi）

**文件：** `go.html` + `js/go.js`（1148 行）

#### 功能说明

| 功能 | 说明 |
|------|------|
| 棋盘规格 | 支持 9×9、13×13（默认）、19×19 |
| 对局模式 | 本地人人对战 / 人机对战 |
| 操作 | 落子、虚手（Pass）、悔棋、认输、重新开始 |
| 计分 | 中国规则实时计分 |

#### 核心实现

- **规则引擎：**
  - 气（liberty）计算：BFS 连通块检测
  - 提子：无气棋子自动移除
  - 禁止自杀：落子后自身无气且无法提子则禁止
  - 打劫检测：恰好提 1 子且己方为 1 子时触发劫争限制
- **计分系统（中国规则）：**
  - 黑方得分 = 黑子数 + 黑方领地
  - 白方得分 = 白子数 + 白方领地 + 贴目（3.75 子）
  - 领地检测：BFS 洪填，空点周围仅有一种颜色则为该方领地
  - 提前终止：当一方理论最高分仍低于对方当前分时自动判负
- **AI 策略：**
  - 提子优先（50 分）
  - 救援危险棋子（60 分）
  - 威胁对手（40 分）
  - 中心偏好
- **渲染：** SVG 网格线渲染（`renderBoardLinesSVG(N)`），传统星位标记

---

### 3.3 消消乐 — 挑战模式（Sheep）

**文件：** `sheep.html` + `js/sheep.js`（503 行）

#### 功能说明

| 功能 | 说明 |
|------|------|
| 关卡系统 | 7 关递进难度 |
| 进度保存 | localStorage 持久化最高通关等级 |
| 通关证书 | 完成全部 7 关后颁发电子证书 |

#### 关卡设计

| 关卡 | 层数 | 网格 | 匹配数 | 槽位数 |
|------|------|------|--------|--------|
| 1-2 | 3 层 | 3×3 | 3 消 | 7 |
| 3-4 | 5 层 | 4×3 | 3 消 | 7 |
| 5-6 | 7 层 | 4×3 | 3 消 | 7 |
| 7 | 9 层 | 5×2 | **5 消** | **10** |

#### 核心实现

- **卡牌遮挡：** `isTop()` 函数检测上层卡牌是否覆盖当前卡牌，被覆盖则不可点击
- **随机布局：** 位置生成带抖动（jitter）参数，营造自然堆叠效果
- **计时系统：** 全局计时器累计所有关卡用时
- **证书系统：**
  - 显示通关日期、格式化用时
  - 隐藏入口：三连击标题或 URL hash 为 `#cert` 可预览证书
- **数据持久化：** `sheep_highest_level`（最高通关等级）、`sheep_passed`（已通过关卡列表）

---

### 3.4 消消乐 — 简易模式（Sheep Simple）

**文件：** `sheep-simple.html` + `js/sheep-simple.js`（331 行）

#### 功能说明

| 功能 | 说明 |
|------|------|
| 单局游戏 | 固定配置，无需闯关 |
| 无限悔棋 | 任意次撤回上一步 |
| 无限洗牌 | 随机打乱剩余卡牌类型 |

#### 核心实现

- **固定配置：** 6×5 网格、3 层、3 消、7 槽位
- **卡牌数量：** 从 [69, 72, 75, 78, 81] 中随机选取
- **图案分配：** 从 9 种图案中随机选 8 种，每种保证 ≥3 张（确保可消除）
- **遮挡检测：** `isCardBlocked()` 检查高层卡牌是否覆盖
- **悔棋栈：** `lastActions` 栈记录操作历史
- **洗牌逻辑：** 随机重新分配剩余卡牌的图案类型

---

### 3.5 足球比分预测（Football Prediction）

**文件：** `football.html` + `js/football.js`（1144 行）

#### 功能说明

| 功能 | 说明 |
|------|------|
| 球队输入 | 支持中文队名，自动映射到真实球队 |
| 比分预测 | 基于泊松分布的比分预测 |
| 胜率分析 | 胜/平/负概率可视化 |
| 历史战绩 | 展示双方近 5 场交锋 |
| 数据来源 | 优先使用真实数据，不足时回退到模拟数据 |

#### 核心实现

- **球队映射：** 100+ 中文队名 → 英文名 + 联赛代码，覆盖 9 大联赛
  - 联赛：英超、西甲、德甲、意甲、法甲、葡超、荷甲、苏超、比甲
- **数据管线：**
  1. 从 GitHub `openfootball/football.json` 拉取真实比赛数据
  2. 提取目标球队的真实历史战绩
  3. 不足 5 场时用模拟数据补齐
  4. 数据来源标记：`real`（全真实）/ `mixed`（混合）/ `simulated`（全模拟）
- **预测模型：**
  - 泊松采样：`poissonSample(lambda, rng)` 生成进球数
  - 权重组合：55% 近期状态 + 25% 攻击力 + 20% 对手防守力
  - 主场优势系数 ×1.1
  - 交锋历史修正 ±8%
  - 胜率矩阵：泊松分布计算 0-7 球所有比分的概率
- **缓存机制：** localStorage 缓存，5 分钟 TTL，6 秒请求超时
- **回退策略：** 无映射 → 立即回退；拉取失败 → 回退；无结果 → 回退
- **随机模型：** FNV-1a 哈希种子 + LCG 伪随机数生成器，保证同队名结果一致

---

### 3.6 计算器（Calculator）

**文件：** `calculator.html` + `js/calculator.js`（237 行）

#### 功能说明

| 功能 | 说明 |
|------|------|
| 基本运算 | 加、减、乘、除 |
| 特殊运算 | 百分比（%） |
| 操作 | C（清空）、⌫（退格）、=（计算） |
| 异常处理 | 除零显示 "Error" |

#### 核心实现

- **精度控制：** `parseFloat(result.toFixed(10))` 避免浮点误差
- **自适应字号：** 数字长度 >12 字符 → 32px，>9 字符 → 40px，其余 48px
- **交互特效：** 触摸涟漪效果，点击缩放动画（0.92 → 1）

---

## 四、公共模块

### 4.1 动态背景管理器（Background Manager）

**文件：** `js/background-manager.js`（116 行）

- 5 张风景图 + 5 组渐变备选方案
- 不重复随机选取（确保与当前不同）
- 图片预加载：`onload` / `onerror` 回调处理
- 全局 API：`window.BackgroundManager.init()` / `.next()` / `.getCurrent()`

### 4.2 气球/气泡交互特效（Balloon Bubble Interaction）

**文件：** `js/balloon-bubble-interaction.js`（261 行）

- **气球：** 点击 → 放大 scale(1.3) → 随机延迟 200-500ms → 爆破 → 6-8 片碎片带重力下落
- **气泡：** 点击 → 立即爆破 → 8-12 颗粒子向外辐射
- 爆破后自动重建新元素
- 8 种气球颜色映射
- 主页使用内联版本，此文件为独立模块版本

### 4.3 本地服务器启动脚本（start.bat）

**文件：** `start.bat`（44 行）

- **主方案：** Python `http.server 8080`
- **备选方案：** PowerShell `HttpListener`（处理 MIME 类型映射）
- 默认路由：`/` → `/index.html`

---

## 五、技术架构总结

| 维度 | 方案 |
|------|------|
| 前端技术 | HTML5 + CSS3 + 原生 JavaScript（ES5/ES6） |
| 依赖 | 零依赖，无框架 |
| 数据持久化 | localStorage |
| 跨标签通信 | localStorage 轮询（五子棋联机） |
| 图形渲染 | Canvas（五子棋）/ SVG（围棋）/ DOM（消消乐、计算器） |
| AI 实现 | 启发式评估函数（五子棋、围棋）/ 泊松分布模型（足球预测） |
| 响应式 | viewport meta + 触摸事件 + pointerenter 跨设备输入 |
| 启动方式 | start.bat → Python/PowerShell 本地服务器（端口 8080） |
